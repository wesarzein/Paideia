import { Component, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ApiService, CoursePayload } from '../../core/services/api.service';
import { PageHeaderComponent } from '../../shared/components/page-header/page-header.component';

type Course = { id: string; name: string; code: string };
@Component({ standalone: true, imports: [FormsModule, PageHeaderComponent], template: `<app-page-header title="Cursos" description="Administra las áreas y cursos del plan académico." /><section class="panel"><form (ngSubmit)="save()"><div class="grid"><label>Nombre del curso<input name="name" [(ngModel)]="form.name" required /></label><label>Código<input name="code" [(ngModel)]="form.code" required /></label></div><button type="submit">{{ editingId ? 'Guardar cambios' : 'Crear curso' }}</button>@if (editingId) {<button type="button" class="muted" (click)="reset()">Cancelar</button>}</form></section><section class="panel table-wrap"><table><thead><tr><th>Curso</th><th>Código</th><th>Acciones</th></tr></thead><tbody>@for (course of courses(); track course.id) {<tr><td>{{ course.name }}</td><td>{{ course.code }}</td><td><button class="small" type="button" (click)="edit(course)">Editar</button><button class="small danger" type="button" (click)="remove(course)">Eliminar</button></td></tr>}</tbody></table></section>`, styles: [` .panel{background:var(--color-surface);border:1px solid var(--color-border);border-radius:8px;padding:22px;margin-bottom:18px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}label{display:grid;gap:7px;color:var(--color-muted-text)}input{padding:10px;border:1px solid var(--color-border);border-radius:6px;font:inherit}button{margin:16px 8px 0 0;background:var(--color-primary);color:white;border:0;border-radius:6px;padding:10px 14px;cursor:pointer}.muted{background:var(--color-muted-text)}.table-wrap{overflow:auto}table{width:100%;border-collapse:collapse}th,td{padding:12px;border-bottom:1px solid var(--color-border);text-align:left}.small{padding:7px 10px;margin:0 5px 0 0}.danger{background:#b94b3b}@media(max-width:700px){.grid{grid-template-columns:1fr}} `] })
export class CoursesPage { private readonly api = inject(ApiService); protected readonly courses = signal<Course[]>([]); protected form: CoursePayload = { name: '', code: '' }; protected editingId: string | null = null; constructor() { this.load(); } private load() { this.api.listCourses().subscribe({ next: (data) => this.courses.set(data as Course[]) }); } protected edit(course: Course) { this.editingId = course.id; this.form = { name: course.name, code: course.code }; } protected reset() { this.editingId = null; this.form = { name: '', code: '' }; } protected save() { const request = this.editingId ? this.api.updateCourse(this.editingId, this.form) : this.api.createCourse(this.form); request.subscribe({ next: () => { this.reset(); this.load(); } }); } protected remove(course: Course) { if (confirm(`¿Eliminar ${course.name}?`)) this.api.deleteCourse(course.id).subscribe({ next: () => this.load() }); } }
